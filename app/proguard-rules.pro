# File di regole ProGuard/R8 aggiuntive.
# Vuoto per ora: la minificazione è disattivata (isMinifyEnabled = false)
# sia in debug che in release in questa prima versione, quindi queste
# regole non vengono ancora applicate. Il file esiste comunque perché
# app/build.gradle.kts lo referenzia — se in futuro si attiva la
# minificazione per le build di release, le regole personalizzate
# andranno aggiunte qui.
